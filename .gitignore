# Secrets — never commit
.env
.env.*
!.env.example

# Personal Claude Code overrides for this project
.claude/settings.local.json

# OS / editor noise
.DS_Store
Thumbs.db
*.swp

# Node (from Iteration 2 on, when the app lands here)
node_modules/
npm-debug.log*
